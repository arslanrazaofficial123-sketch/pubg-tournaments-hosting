import { SignInForm } from "@/features/auth";

export const metadata = {
  title: "Link UID | EPIX Esports",
  description: "Link your PUBG Mobile UID to access EPIX Esports.",
};

export default function LinkUidPage() {
  return <SignInForm />;
}
